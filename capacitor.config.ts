import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PJTUT2-FrontEnd',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
